apiVersion: v1
kind: Service
metadata:
  name: {{ include "aerospike-ce-kubernetes-operator.metricsServiceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aerospike-ce-kubernetes-operator.labels" . | nindent 4 }}
spec:
  ports:
    - name: https
      port: 8443
      protocol: TCP
      targetPort: 8443
  selector:
    {{- include "aerospike-ce-kubernetes-operator.selectorLabels" . | nindent 4 }}
