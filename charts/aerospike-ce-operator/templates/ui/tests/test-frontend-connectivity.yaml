{{- if and .Values.ui.enabled .Values.ui.tests.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "aerospike-ce-operator.ui.fullname" . }}-test-frontend
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aerospike-ce-operator.ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  securityContext:
    runAsNonRoot: true
    runAsUser: 100
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: curl-test
      image: curlimages/curl:8.5.0
      command:
        - sh
        - -c
        - |
          echo "Testing frontend endpoint..."
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            --max-time 10 \
            http://{{ include "aerospike-ce-operator.ui.fullname" . }}:{{ .Values.ui.service.frontendPort }}/)
          echo "HTTP status code: $HTTP_CODE"
          if [ "$HTTP_CODE" = "200" ]; then
            echo "Frontend connectivity check passed!"
            exit 0
          else
            echo "Frontend connectivity check failed with HTTP $HTTP_CODE"
            exit 1
          fi
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        capabilities:
          drop:
            - ALL
      resources:
        requests:
          cpu: 10m
          memory: 32Mi
        limits:
          cpu: 50m
          memory: 64Mi
{{- end }}
