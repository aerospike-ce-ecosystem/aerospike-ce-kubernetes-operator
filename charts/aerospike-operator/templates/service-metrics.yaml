apiVersion: v1
kind: Service
metadata:
  name: {{ include "aerospike-operator.metricsServiceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aerospike-operator.labels" . | nindent 4 }}
spec:
  ports:
    - name: https
      port: 8443
      protocol: TCP
      targetPort: 8443
  selector:
    {{- include "aerospike-operator.selectorLabels" . | nindent 4 }}
