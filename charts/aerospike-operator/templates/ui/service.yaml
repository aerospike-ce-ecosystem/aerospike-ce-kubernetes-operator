{{- if .Values.ui.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "aerospike-operator.ui.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aerospike-operator.ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - name: frontend
      port: {{ .Values.ui.service.frontendPort }}
      targetPort: frontend
      protocol: TCP
      appProtocol: http
    - name: backend
      port: {{ .Values.ui.service.backendPort }}
      targetPort: backend
      protocol: TCP
      appProtocol: http
  selector:
    {{- include "aerospike-operator.ui.selectorLabels" . | nindent 4 }}
{{- end }}
