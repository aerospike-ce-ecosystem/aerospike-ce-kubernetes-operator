apiVersion: acko.io/v1alpha1
kind: AerospikeClusterTemplate
metadata:
  name: hard-rack
  namespace: default
  labels:
    app.kubernetes.io/profile: hard-rack
spec:
  # Hard-rack template: standard Aerospike image and cluster size for production
  image: aerospike:ce-8.1.1.1   # Default image; clusters can override with spec.image
  size: 6                         # Default cluster size; clusters can override with spec.size

  # Monitoring: Prometheus exporter sidecar with ServiceMonitor
  monitoring:
    enabled: true
    port: 9145
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 128Mi
    serviceMonitor:
      enabled: true
      interval: 30s

  # Default network access policy
  aerospikeNetworkPolicy:
    accessType: pod
    alternateAccessType: pod
    fabricType: pod

  # Hard-rack: full resources, required anti-affinity, local PV
  scheduling:
    podAntiAffinityLevel: required   # Hard anti-affinity: one pod per node
    tolerations:
      - key: "aerospike"
        operator: "Exists"
        effect: "NoSchedule"
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: "aerospike"
                operator: In
                values:
                  - "true"

  storage:
    storageClassName: local-path     # Local storage for performance
    localPVRequired: true            # Enforce WaitForFirstConsumer binding
    resources:
      requests:
        storage: 100Gi

  resources:
    requests:
      cpu: "2"
      memory: "4Gi"
    limits:
      cpu: "2"              # Guaranteed QoS: requests == limits
      memory: "4Gi"

  rackConfig:
    maxRacksPerNode: 1   # One rack per node (enforced with required anti-affinity)

  aerospikeConfig:
    service:
      proto-fd-max: 15000
    namespaceDefaults:
      memory-size: 2147483648   # 2GiB default per namespace
      replication-factor: 2
    network:
      heartbeat:
        mode: mesh
        interval: 150
        timeout: 10
