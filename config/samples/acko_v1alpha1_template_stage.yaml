apiVersion: acko.io/v1alpha1
kind: AerospikeCEClusterTemplate
metadata:
  name: stage
  namespace: default
  labels:
    environment: stage
spec:
  # Stage template: moderate resources, preferred anti-affinity
  scheduling:
    podAntiAffinityLevel: preferred   # Soft anti-affinity for better spread
    tolerations:
      - key: "aerospike"
        operator: "Exists"
        effect: "NoSchedule"

  storage:
    storageClassName: standard
    resources:
      requests:
        storage: 10Gi

  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "2"
      memory: "2Gi"

  aerospikeConfig:
    service:
      proto-fd-max: 4096
    namespaceDefaults:
      replication-factor: 2
      # memory-size is obsolete in CE 8.x
    network:
      heartbeat:
        mode: mesh
        interval: 150
        timeout: 10
